# SignalBoard（信号板）

SignalBoard（信号板）是一个本地桌面端项目概况板：在众多并行的 vibecoding 项目中，自动聚合各项目的事实近况，帮助使用者快速回忆起「每个项目做到哪了」，防止遗忘导致的决策漂移。核心是事实聚合，不是项目管理平台；在此之上向 Agent 配合延伸（ADR-0004）：意图路由把警示与建议翻译成 prompt 交给本机 AI CLI 执行，MCP 接口让 agent 查询事实、读写交接。

## Language

**项目（Project）**:
扫描根目录下任何含 `.git` 的目录（黑名单除外），外加逐项添加的扫描外补充路径。工具自身也是一个项目。
_Avoid_: 仓库（repo 是实现细节）、工作区

**近况信号（Signals）**:
从项目自动提取的事实数据：最后提交时间、近7天提交数、最近提交消息、未提交改动数、未提交改动的最近修改时间、当前分支、领先/落后远程、全年提交活动热力图、AI 会话痕迹。全部客观、零维护。
_Avoid_: 状态（status，过于模糊）、指标

**AI 会话痕迹（AI Session Trace）**:
各 AI 工具（claude / codex / kimi / grok）在该项目下的最近会话时间，用于标记「最近有 AI 会话但可能未提交」的项目。探测位同时覆盖用户目录会话位（`~/.claude/projects/<路径转义>`、`~/.kimi-code/sessions/wd_*` 按 cwd 精确匹配、`~/.codex/sessions` rollout 首行 cwd、`~/.grok/sessions/<路径编码>`）与旧版项目本地目录（`.kimi-code`/`.claude`/`.codex`/`.grok`），取较新者（issue #35）。
_Avoid_: AI 记录、会话日志

**备忘（Memo）**:
每个项目一段人工编辑的纯文本，展示在面板上该项目最显眼的位置。是防决策漂移的唯一人工钩子，不属于近况信号。agent 不写备忘——agent 的留言走交接。
_Avoid_: 笔记、备注、标签

**交接（Handoff）**:
agent 完成一段工作后随项目写下的留言，供下一个执行者（人或 agent）接力；经 MCP 接口读写。与备忘并列而不合并：备忘是人写给人防决策漂移，交接是 agent 写给下一个执行者做跨会话接力。
_Avoid_: 日志、留言板

**活跃分带（Activity Band）**:
按项目最后活动时间划分的五个分组：活跃（3天内）、近期（3-7天）、渐冷（7-30天）、沉睡（30-90天）、归档（90天+）。「最后活动时间」取最后提交时间、AI 会话痕迹时间、未提交改动文件修改时间的最大值；已议定（v0.4，ADR-0004）：交接写入时间同级计入。组内排序规则见主面板。
_Avoid_: 阶段、状态分类

**警示标记（Warning Flag）**:
附加在项目上的注意力信号：未提交改动超3天、领先远程未推送、有开放 PR。已议定扩编（v0.3，ADR-0004）：默认分支 CI 失败、PR 有待处理 review。
_Avoid_: 告警、提醒

**警示消音（Warning Snooze）**:
用户对单条警示标记的主动忽略。被消音的警示不再出现在需要关注清单，直到其底层状态变化（数量或内容改变）后自动复出。
_Avoid_: 已读、屏蔽

**需要关注（Attention List）**:
所有带警示标记（未消音）的项目的汇总清单，位于总览页左栏首屏主角位（深色锚点卡），是全应用视觉权重最高的区域；点击条目跳转主面板并推出该项目详情。
_Avoid_: 待办（todo，暗示用户手动维护的任务列表）

**主攻项目（Pinned Focus）**:
用户图钉的项目，在主面板行列表中优先置顶显示；未图钉时按默认位序排列。
_Avoid_: 置顶项目、收藏

**项目位序（Item Order）**:
主面板行列表的排列规则。提供可选的排序方式：手动（默认）、按最近活跃、按名称。手动方式下默认按项目名稳定排序，用户可拖拽重排，重排结果持久化；切到其余方式时仅作为视图覆盖，不破坏已保存的手动位序，切回即恢复。
_Avoid_: 智能排序、动态布局

**总览（Overview）**:
主窗口顶层视图之一，与主面板并列，导航位序在其左。聚合跨项目的全局事实：全年贡献热力图、按月的活动流，以及常用工作台入口。不含单项目细节。
_Avoid_: 首页、仪表盘

**主面板（Board）**:
主窗口顶层视图之一（项目列表视图），按项目位序组织项目行，活跃分带作为页内筛选器。点击项目行在主窗口右侧推出详情面板。
_Avoid_: 仪表盘（dashboard，暗示图表与平台化）

**详情面板（Detail Panel）**:
主面板右侧按需推出的单项目深区，自上而下：头部（分带 pill + 项目名）、路径行（含复制路径按钮）、备忘编辑、快捷打开、近况信号、警示消音、AI 建议、月份热力图（单月日历视图，可翻页）、近期提交、未提交文件、「更多事实」折叠组（README 摘要 / AI 会话痕迹明细 / issues/PR，默认折叠，issue #83）。未展开时主面板为单列行列表。
_Avoid_: 抽屉、弹窗

**活动流（Activity Stream）**:
总览页右栏主区按月份分组的跨项目提交事实流：每月一节，列出涉及项目、提交数与相对比例。仅含可自动提取的事实。
_Avoid_: 动态、时间线

**工作台（Workbench）**:
总览页左栏的 AI 工具快捷启动区（按本机安装情况显隐）。仅聚合本机能力，不含手工维护内容。
_Avoid_: 挂件、小组件

**快捷打开（Quick Open）**:
从详情面板一键打开项目文件夹、用可配置的编辑器打开、或在项目目录打开终端；含 AI 工具启动——在本机已安装的前提下，于项目目录启动 claude、codex、kimi、grok 等 Agent 命令行工具。
_Avoid_: 跳转、快捷方式

**意图路由（Intent Routing）**:
把警示标记或 AI 建议翻译成预填 prompt，一键在项目目录的可见终端起 AI CLI 执行；SignalBoard 自身保持磁盘只读，写操作由使用者自己的 AI 会话完成。首发范围：三类警示固定模板 + AI 建议条目执行键；通用入口留接口不实现。
_Avoid_: 自动执行、无人值守

**AI 引擎（AI Engine）**:
AI 功能调用的本机 Agent CLI：候选为已探测可用的 AI 工具，设置里可指定默认项，缺省取第一个可用。所有调用经主进程 `ai:ask` 统一组装 prompt、90 秒超时、失败降级、结果缓存；首选引擎失败后自动链式回退其余已安装引擎，并记忆最近可用引擎优先使用（issue #41）；只发送统计事实与提交信息文本，不上传代码内容。生成任务在后台执行，切换页面不中断、不重复发起（issue #40）。
_Avoid_: 模型、API

**AI 周报（Weekly Summary）**:
总览页独立模块的本机 AI 生成摘要：近 7 天跨项目提交事实 → 一段自然语言总结 + 一句「本周建议关注」。打开应用即有：今日缓存命中直接展出，无缓存则启动后自动后台生成一次（失败保持安静，可手动再生成）（issue #42）。结果按当天日期缓存复用。模块内标明生成引擎与生成时间（issue #32；徽标仅「引擎 + 时间」，issue #43）。
_Avoid_: 报告、日报

**AI 建议（AI Advice）**:
详情面板内按需生成的 2–3 条下一步行动建议，输入为该项目 git 信号事实，结果按 项目+HEAD+模板哈希+事实摘要签名（ahead/behind/未提交数/警示标签）缓存——push 等不改变 HEAD 的事实变化也会使旧缓存失效（issue #131）。
_Avoid_: 智能推荐

## Boundaries

桌面应用形态：托盘常驻、开机自启、全局热键唤出；唤出着陆视图可在「总览/项目/上次停留」间配置（issue #86）。通知静默为主，每天首次启动或唤出窗口时一条警示摘要（issue #51 改为唤出兜底）。视觉语言以 `前端设计参考/案例2.webp` 为基底，落地为多主题体系：设置页「外观」提供暖阳/雾蓝/青野/樱粉/紫藤/暗夜/夜幕七套整体主题预设（含两套深色主题）与自定义强调色，选择即生效（issue #27，扩充见 issue #68）；主题可设为「自动」跟随系统明暗在浅/深主题对间切换（issue #81）；密度有标准/紧凑两档（issue #84）；可降低动效——停动画并把玻璃模糊退化为不透明实底，同时跟随系统 prefers-reduced-motion（issue #82）。v1 不做：插件系统、多设备同步、用户认证、数据库；通用 MCP client（任意外部 server 透传）暂缓，待第二个非 GitHub 需求出现再议（ADR-0004）。GitHub 集成仅覆盖 owner 是使用者的仓库；fork 的上游仓库跳过；数据面经 REST 直连扩展（PR review 状态与评论、CI 运行、通知、Release、仓库元数据等），不经 MCP，通知首版仅收本人仓库（ADR-0004）。GitHub 连接导入即持久化登录名，数据拉取落地后立即推补丁展出，无需重启（issue #44）；设置页展示账户头像、用户名与连通性，可断开（issue #45）。AI 周报/AI 建议的提示词模板可在设置中自定义（{{事实}} 插入点注入实时事实，事实块组装留在代码里，缓存键随模板内容失效，issue #78）。备忘、位序、消音与偏好可手动导出/导入为单个 JSON 文件（token 不随包迁移），作为换机/重装的最低成本替身（v1 不做多设备同步，issue #79）。Agent 配合边界（ADR-0004）：SignalBoard 保持磁盘只读，不写项目磁盘；行动一律经意图路由交给使用者自己的 AI 会话在可见终端执行（人在环），不做无人值守的自动调度；MCP server（v0.4）向 agent 暴露近况查询、交接读写与动作工具（触发重扫 / 快捷打开 / 起 CLI），备忘与警示消音保持人工操作。
