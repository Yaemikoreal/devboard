# devboard

devboard 是一个本地桌面端项目概况板：在众多并行的 vibecoding 项目中，自动聚合各项目的事实近况，帮助使用者快速回忆起「每个项目做到哪了」，防止遗忘导致的决策漂移。核心是事实聚合，不是项目管理平台。

## Language

**项目（Project）**:
扫描根目录下任何含 `.git` 的目录（黑名单除外），外加逐项添加的扫描外补充路径。工具自身也是一个项目。
_Avoid_: 仓库（repo 是实现细节）、工作区

**近况信号（Signals）**:
从项目自动提取的事实数据：最后提交时间、近7天提交数、最近提交消息、未提交改动数、未提交改动的最近修改时间、当前分支、领先/落后远程、全年提交活动热力图、AI 会话痕迹。全部客观、零维护。
_Avoid_: 状态（status，过于模糊）、指标

**AI 会话痕迹（AI Session Trace）**:
项目目录下 AI 工具会话目录（如 `.kimi-code`、`.claude`）的最近活动时间，用于标记「最近有 AI 会话但可能未提交」的项目。
_Avoid_: AI 记录、会话日志

**备忘（Memo）**:
每个项目一段人工编辑的纯文本，展示在面板上该项目最显眼的位置。是防决策漂移的唯一人工钩子，不属于近况信号。
_Avoid_: 笔记、备注、标签

**活跃分带（Activity Band）**:
按项目最后活动时间划分的五个分组：活跃（3天内）、近期（3-7天）、渐冷（7-30天）、沉睡（30-90天）、归档（90天+）。「最后活动时间」取三者的最大值：最后提交时间、AI 会话痕迹时间、未提交改动的文件修改时间。组内排序规则见主面板。
_Avoid_: 阶段、状态分类

**警示标记（Warning Flag）**:
附加在项目上的注意力信号：未提交改动超3天、领先远程未推送、有开放 PR。
_Avoid_: 告警、提醒

**警示消音（Warning Snooze）**:
用户对单条警示标记的主动忽略。被消音的警示不再出现在需要关注清单，直到其底层状态变化（数量或内容改变）后自动复出。
_Avoid_: 已读、屏蔽

**需要关注（Attention List）**:
所有带警示标记（未消音）的项目的汇总清单，固定在总览页右栏，是全应用视觉权重最高的区域；点击条目跳转主面板并推出该项目详情。
_Avoid_: 待办（todo，暗示用户手动维护的任务列表）

**主攻项目（Pinned Focus）**:
用户图钉的项目，在主面板行列表中优先置顶显示；未图钉时按默认位序排列。
_Avoid_: 置顶项目、收藏

**项目位序（Item Order）**:
主面板行列表的排列规则。提供可选的排序方式：手动（默认）、按最近活跃、按名称。手动方式下默认按项目名稳定排序，用户可拖拽重排，重排结果持久化；切到其余方式时仅作为视图覆盖，不破坏已保存的手动位序，切回即恢复。
_Avoid_: 智能排序、动态布局

**总览（Overview）**:
主窗口顶层视图之一，与主面板并列，导航位序在其左。聚合跨项目的全局事实：全年贡献热力图、按月的活动流，以及常用工作台入口。不含单项目细节。
_Avoid_: 首页、仪表盘

**主面板（Board）**:
主窗口顶层视图之一（项目列表视图），按项目位序组织项目行，活跃分带作为页内筛选器。点击项目行在主窗口右侧推出详情面板。
_Avoid_: 仪表盘（dashboard，暗示图表与平台化）

**详情面板（Detail Panel）**:
主面板右侧按需推出的单项目深区：备忘编辑、近况信号、警示消音、全年热力图、近期提交、未提交文件、issues/PR、README 摘要、AI 会话痕迹明细与快捷打开。未展开时主面板为单列行列表。
_Avoid_: 抽屉、弹窗

**活动流（Activity Stream）**:
总览页左栏按月份分组的跨项目提交事实流：每月一节，列出涉及项目、提交数与相对比例。仅含可自动提取的事实。
_Avoid_: 动态、时间线

**工作台（Workbench）**:
总览页右栏的常用入口区，含需要关注清单与 AI 工具快捷启动（按本机安装情况显隐）。仅聚合事实与本机能力，不含手工维护内容。
_Avoid_: 挂件、小组件

**快捷打开（Quick Open）**:
从详情面板一键打开项目文件夹、用可配置的编辑器打开、或在项目目录打开终端；含 AI 工具启动——在本机已安装的前提下，于项目目录启动 claude、codex、kimi、grok 等 Agent 命令行工具。
_Avoid_: 跳转、快捷方式

## Boundaries

桌面应用形态：托盘常驻、开机自启、全局热键唤出；通知静默为主，每日启动时一条警示摘要。视觉语言严格参照 `前端设计参考/案例2.webp`，仅浅色主题。v1 不做：插件系统、多设备同步、用户认证、数据库。GitHub 集成仅覆盖 owner 是使用者的仓库；fork 的上游仓库跳过。
